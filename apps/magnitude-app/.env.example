# LiteLLM Gateway Configuration
# All LLM requests are routed through the local LiteLLM proxy

# LiteLLM API endpoint (default: http://localhost:4000)
LITELLM_BASE_URL=http://localhost:4000

# LiteLLM Master Key for authentication
# Get this from your ai-gateway/.env file (LITELLM_MASTER_KEY)
LITELLM_API_KEY=sk-litellm-master-your-key-here

# Default model to use (must be configured in litellm_config.yaml)
LITELLM_DEFAULT_MODEL=claude-haiku-4-5
